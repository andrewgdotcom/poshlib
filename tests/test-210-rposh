#!/bin/bash
. $(dirname "${BASH_SOURCE[0]}")/../poshlib.sh
use swine
use rposh

if [ "${1:-}" == "" ]; then
    rscript localhost "${BASH_SOURCE[0]}" "last" "test"
elif [ "${1:-}" != "last" ]; then
    die 1 "Mangled arguments to test-rposh"
fi
